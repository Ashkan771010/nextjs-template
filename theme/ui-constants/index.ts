import colors from "./color";
import borderRadiuses from "./border-radius/borderRadiuses";
import paddings from "./padding";
import margins from "./margin";
import breakPoints from "./breakpoints";

import FontFamily, { fonts } from "./fonts";

const theme = {
  colors,
  borderRadiuses,
  paddings,
  margins,
  breakPoints,
  fonts
};

export default theme;
export { FontFamily };
